---
description: Start Development Server and Open on Port 8080
---
The user has explicitly stated that the development server should be accessed via localhost:8080.

1. Run the dev server
// turbo
```powershell
npm run dev
```

2. When opening the browser or checking the app, ALWAYS use port 8080.
   - URL: `http://localhost:8080`
   - Example: `http://localhost:8080/fx-comparison`
